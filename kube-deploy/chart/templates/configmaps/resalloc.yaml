---
apiVersion: v1
kind: ConfigMap
metadata:
  name: copr-resalloc-config
  namespace: {{ include "copr.namespace" . }}
  labels:
    {{- include "copr.labels" . | nindent 4 }}
data:
  pools.yaml: |
{{ .Values.resalloc.poolsYaml | indent 4 }}
